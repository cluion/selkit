# Examples

Interactive examples — pick one from the left, operate the **live demo**, then
switch between **vue**, **vanilla**, and **react** to see the same feature across
frameworks. The live demos run on `@selkit/vue`; all three share one
`@selkit/core` state machine.

<ExampleGallery />
